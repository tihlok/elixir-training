<h1>Listing All Users</h1>

<table>
    <%= for user <- @users do %>
        <tr>
            <td>
                <%= render "user.name.html", user: user %>
            </td>
            <td>
                <%# <%= if is_me?(@conn, user) do %>
                    <%= link "View", to: Routes.user_path(@conn, :show, user.id) %>
                <%# <% end %>
            </td>
        </tr>
    <% end %>
</table>

<%= link "NEW", to: Routes.user_path(@conn, :new) %>
